extends layout

block content
  h1= title
  p Welcome to #{title}

  form(action='/find', method='post')
      label(for="query")
        input(type="text", name='query', id='query')
      br
      br
      input(type='submit', value='Find')

  div
      p #{category}